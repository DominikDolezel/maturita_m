\section{Limita funkce}\label{limita}
\begin{definition}[Vlastní limita ve vlastním bodě]\label{vlimv}
    Funkce $f$ má v bodě $x_0\in \mathbb R$ \textbf{limitu} $A\in \mathbb R,$
    jestliže ke každému $\varepsilon \in \mathbb R^+$ existuje
    $\delta \in \mathbb R^+$ takové, že $\forall x \in
    \left ( x_0-\delta, x_0+\delta \right ), x\ne x_0 $ platí $f(x)\in
    \left ( A-\varepsilon,A+\varepsilon \right ). $ Píšeme
    $$\lim_{x\to x_0}f(x)=A.$$
\end{definition}

\begin{pozn}
    Definici \ref{vlimv} lze také zapsat symbolicky. Řekneme, že
    $\lim_{x\to x_0} f(x) =A$, pokud
    $$
    \forall \varepsilon \in \mathbb R^+\;
        \exists \delta \in \mathbb R^+\; \forall x \in
        \left ( x_0-\delta,x_0+\delta \right )\smallsetminus\left \{ x_0 \right \}\colon
        f(x) \in \left ( A-\varepsilon, A+\varepsilon \right ).
    $$
\end{pozn}

\begin{figure}[ht!]
\centering
\begin{tikzpicture}[>=Stealth, declare function={f(\x)=-0.1*(\x)^2+5;}]
    % Osy
    \draw[->] (-0.5,0) -- (6,0) node[right]{$x$};
    \draw[->] (0,-0.5) -- (0,6) node[above]{$y$};

    % Funkce y = f(x) (klesající, konvexní)
    \draw[thick] plot[domain=0:6, smooth] (\x,{f(\x)}) node[right]{$y = f(x)$};

    % Bod a a jeho projekce
    \def\a{3}
    \def\fa{-0.1*9+5}
    \draw[dashed] (\a,0) node[below]{$a$} -- (\a,\fa+1.7);
    \draw[dashed] (\a+1.7,\fa) -- (0,\fa) node[left]{$A$};
    \filldraw[white, thick] (\a,\fa) circle (2pt);
    \draw[thick] (\a,\fa) circle (2pt);

    % Šipky a popisky pro δ (vlevo i vpravo)
    \draw[<->] (\a-1.5,\fa-1.7) -- (\a,\fa-1.7) node[midway, below]{$\delta$};
    \draw[<->] (\a,\fa-1.7) -- (\a+1.5,\fa-1.7) node[midway, below]{$\delta$};
    \draw[dashed] (\a-1.5,0) -- (\a-1.5,\fa+1.7);
    \draw[dashed] (\a+1.5,0) -- (\a+1.5,\fa+1.7);

    % Šipky a popisky pro ε (nahoru i dolů)
    \draw[<->] (\a+1.7,\fa+1.5) -- (\a+1.7,\fa) node[midway, right]{$\varepsilon$};
    \draw[<->] (\a+1.7,\fa) -- (\a+1.7,\fa-1.5) node[midway, right]{$\varepsilon$};
    \draw[dashed] (\a-1.7,\fa+1.5) -- (\a+1.7,\fa+1.5);
    \draw[dashed] (\a-1.7,\fa-1.5) -- (\a+1.7,\fa-1.5);
\end{tikzpicture}
\caption{Vlastní limita ve vlastním bodě}
\end{figure}

\begin{definition}[Nevlastní limita ve vlastním bodě]\label{nlimv}
Funkce $f$ má v bodě $x_0\in \mathbb R$ \textbf{nevlastní limitu} $+\infty$ (resp.
$-\infty$),
jestliže ke každému $M \in \mathbb R$ existuje
$\delta \in \mathbb R^+$ takové, že $\forall x \in
\left ( x_0-\delta, x_0+\delta \right ), x\ne x_0 $ platí $f(x)>M $
(resp. $f(x)<M$). Píšeme
$$\lim_{x\to x_0}f(x)=\infty \quad(\textrm{resp. } -\infty).$$
\end{definition}

\begin{pozn}
    Definici \ref{nlimv} lze také zapsat symbolicky. Řekneme, že
    $\lim_{x\to x_0} f(x) =\infty$ (resp. $-\infty$), pokud
    $$\forall M \in \mathbb R\; \exists \delta \in \mathbb R^+\; \forall x \in \left ( x_0-\delta,x_0+\delta \right )\smallsetminus\left \{ x_0 \right \}\colon f(x) > M \,\, (\textrm{resp. } f(x) < M).$$
\end{pozn}

\begin{figure}[ht!]
\centering
\begin{tikzpicture}[>=Stealth, declare function={f(\x)=1+1/(\x-3)^2+\x/3;}]
    % Osy
    \draw[->] (-0.5,2) -- (6,2) node[right]{$x$};
    \draw[->] (0,1.5) -- (0,6) node[above]{$y$};

    % Funkce y = f(x) (s vertikální asymptotou v x = 3)
    \draw[thick] plot[domain=0:2.555, smooth] (\x,{f(\x)});
    \draw[thick] plot[domain=3.455:5.5, smooth] (\x,{f(\x)}) node[right]{$y = f(x)$};

    % Bod a (asymptota)
    \def\a{3}
    \draw[dashed] (\a,2) node[below]{$x_0$} -- (\a,7);

    % Šipky a popisky pro δ (vlevo i vpravo od a)
    \draw[<->] (\a-1,4.3) -- (\a,4.3) node[midway, below]{$\delta$};
    \draw[<->] (\a,4.3) -- (\a+1,4.3) node[midway, below]{$\delta$};
    \draw[dashed] (\a-1,4.3) -- (\a-1,7);
    \draw[dashed] (\a+1,4.3) -- (\a+1,7);

    % Popisek K (pro f(x) > K)
    \draw[dashed] (0,4.5) node[left]{$M$} -- (5.5,4.5);
\end{tikzpicture}
\caption{Nevlastní limita ve vlastním bodě}
\end{figure}

\begin{definition}[Vlastní limita v nevlastním bodě]\label{vlimn}
Funkce $f$ má v bodě $\infty$ (resp. $-\infty$) \textbf{limitu} $A\in \mathbb R,$
jestliže ke každému $\varepsilon \in \mathbb R^+$ existuje
$K \in \mathbb R$ takové, že $\forall x \in \mathbb R, x > K$ (resp. $x<K$)
platí $f(x)\in \left ( A-\varepsilon,A+\varepsilon \right ). $ Píšeme
\begin{align*}
\lim_{x\to \infty}f(x)=A, & & \textrm{resp. }  && \lim_{x\to -\infty}f(x)=A.
\end{align*}
\end{definition}

\begin{pozn}
Definici \ref{vlimn} lze také zapsat symbolicky. Řekneme, že
$\lim_{x\to \infty} f(x) =A$ (resp. $\lim_{x\to -\infty} f(x) =A$), pokud
$$
\forall \varepsilon \in \mathbb R^+\;
    \exists K \in \mathbb R\; \forall x \in \mathbb R, x > K \,\, (\textrm{resp. } x< K)\colon
    f(x) \in \left ( A-\varepsilon, A+\varepsilon \right ).
$$
\end{pozn}

\begin{figure}[ht!]
\centering
\begin{tikzpicture}[>=Stealth, declare function={f(\x)=2+1/(\x);}]
    % Osy
    \draw[->] (-0.5,0) -- (6,0) node[right]{$x$};
    \draw[->] (0,-0.5) -- (0,4) node[above]{$y$};

    % Funkce y = f(x) (přibližující se k A = 2)
    \draw[thick] plot[domain=0.35:5.5, smooth] (\x,{f(\x)}) node[right]{$y = f(x)$};

    % Asymptota y = A
    \def\A{2}
    \draw[dashed] (0,\A) node[left]{$A$} -- (5.5,\A);

    % Šipky a popisky pro ε (nahoru i dolů od A)
    \draw[<->] (3.3,\A) -- (3.3,\A-0.8) node[midway, left]{$\varepsilon$};
    \draw[<->] (3.3, \A) -- (3.3, \A + 0.8) node[left]{$\varepsilon$};
    \draw[dashed] (3.3,\A+0.8) -- (6.5,\A+0.8);
    \draw[dashed] (3.3,\A-0.8) -- (6.5,\A-0.8);

    % Popisek M (pro x > M)
    \draw[dashed] (3.5,0) node[below]{$K$} -- (3.5,\A+1);
\end{tikzpicture}
\caption{Vlastní limita v nevlastním bodě}
\end{figure}

\begin{pozn}
    Definice nevlastní limity v nevlastním bodě jsou celkem čtyři (dvakrát
    dvě možnosti pro plus/minus nekonečno).
    Pro ušetření místa budou oba případy zaznačeny jako $\pm \infty$.
\end{pozn}

\begin{definition}[Nevlastní limita v nevlastním bodě]\label{nlimn}
    Funkce $f$ má v bodě $\pm \infty$ \textbf{limitu} $\pm \infty,$
    jestliže ke každému $M \in \mathbb R$ existuje
    $K \in \mathbb R$ takové, že $\forall x \in
    \mathbb R, x> K $ (resp. $x<K$) platí $f(x)>M$ (resp. $f(x) <M$). Píšeme
    $$\lim_{x\to \pm\infty}f(x)=\pm \infty.$$
\end{definition}

\begin{pozn}
    Definici \ref{nlimn} lze také zapsat symbolicky. Řekneme, že
    $\lim_{x\to \pm\infty} f(x) =\pm\infty$, pokud
    $$
    \forall M \in \mathbb R\;
        \exists K \in \mathbb R\; \forall x \in \mathbb R, x>K \,\, (\textrm{resp. }x<K)\colon
        f(x) > M \,\, (\textrm{resp. } f(x)<M).
    $$
\end{pozn}

\begin{figure}[ht!]
	\centering
	\begin{tikzpicture}[>=Stealth, declare function={f(\x)=0.2*(\x)^2;}]
    % Osy
    \draw[->] (-0.5,0) -- (6,0) node[right]{$x$};
    \draw[->] (0,-0.5) -- (0,6) node[above]{$y$};

    % Funkce y = f(x) (kvadratická, rostoucí do +∞)
    \draw[thick] plot[domain=0:5.5, smooth] (\x,{f(\x)}) node[right]{$y = f(x)$};

    % Šipky a popisky pro M (na ose x)
    \draw[dashed] (4,0) node[below]{$K$} -- (4,5.5);
    \draw[dashed] (0,5) node[left]{$M$} -- (5.5,5);

	\end{tikzpicture}
	\caption{Nevlastní limita v nevlastním bodě}
\end{figure}

\begin{definition}
\textbf{Okolím bodu}
\begin{enumerate}[$i.$]
\item $x_0\in \mathbb R$ rozumíme množinu $(x_0-\delta,x_0+\delta), \delta \in \mathbb R^+,$
\item $\infty$ rozumíme množinu $(k,\infty), k\in \mathbb R,$
\item $-\infty$ rozumíme množinu $(-\infty,k), k\in \mathbb R$
\end{enumerate}
a značíme $\mathscr O(x_0)$ (resp. $\mathscr O(\infty)$). \textbf{Prstencovým okolím
bodu} $x$ je množina $\mathscr O(x)\smallsetminus\left \{ x \right \} $, kterou značíme $\mathscr P(x).$
Množinu $\mathbb R \cup \left \{ \pm \infty \right \} $ nazvěme \textbf{rozšířenou množinou
reálných čísel} a označme $\mathbb R^*.$
\end{definition}

\begin{definition}[Souhrnná definice limity]\label{limsouhr}
Řekneme, že $f$ má v bodě $x_0\in \mathbb R^*$ limitu $A \in \mathbb R^*$,
jestliže ke každému okolí $\mathscr O(A)$ bodu $A$ existuje prstencové
okolí $\mathscr P(x_0)$ bodu $x_0$ takové, že pro všechna $x\in \mathscr P(x_0)$
platí $f(x)\in \mathscr O(A).$ Píšeme
$$
\lim_{x\to x_0}f(x)=A.
$$
\end{definition}

\begin{pozn}
    Definici \ref{limsouhr} lze také zapsat symbolicky. Řekneme, že
    $\lim_{x\to x_0} f(x) = A$, pokud
    $$
    \forall \mathscr O(A)\;
        \exists \mathscr P(x_0)\; \forall x \in \mathscr P(x_0)\colon
        f(x) \in \mathscr O(A).
    $$
\end{pozn}
\begin{veta}
    Funkce $f$ má v bodě $x_0\in \mathbb R^*$ nejvýše jednu limitu.
\end{veta}

\begin{definition}
Funkce $f$ je \textbf{spojitá v bodě} $x_0\in \mathbb R,$ jestliže
$$\lim_{x\to x_0}f(x)=f(x_0).$$
\end{definition}

\begin{pozn}
	Pojmy limity i spojitosti se dále používají i v jednostranné formě, tj. zleva a zprava. V definicích se pak podmínky omezí u limity na levé, resp. pravé prstencové okolí bodu $x_0$ (tedy na levou, resp. pravou polovinu prstencového okolí) a u spojitosti na rovnost limity zleva, resp. limity zprava.
\end{pozn}

\begin{definition}
Funkce $f$ je \textbf{spojitá na intervalu} $J \subseteq \mathbb R,$ jestliže
\begin{enumerate}[$i.$]
\item $f$ je spojitá v každém vnitřním bodě intervalu $J,$
\item patří-li počáteční (resp. koncový) bod $J$ k tomuto intervalu,
je v něm funkce $f$ spojitá zprava (resp. zleva).
\end{enumerate}
\end{definition}

\begin{pozn}[Neurčité výrazy]
Neurčité výrazy jsou
$$\frac{0}{0},  \quad \frac{\infty}{\infty}, \quad 0\cdot \infty, \quad \infty - \infty, \quad0^0,\quad \infty^0,\quad 1^\infty.$$
\end{pozn}

\begin{veta}
Nechť $x_0\in \mathbb R^*$ a nechť existuje $\lim_{x\to x_0}f(x)$ a $\lim_{x\to x_0}g(x)$.
Pak platí
\begin{enumerate}[$i.$]
\item $\lim_{x\to x_0} \left ( f(x)\pm g(x) \right )= \lim_{x\to x_0}f(x) \pm \lim_{x\to x_0}g(x),$
\item $\lim_{x\to x_0}f(x)g(x)=\lim_{x\to x_0}f(x)\cdot \lim_{x\to x_0}g(x),$
\item $\lim_{x\to x_0}\frac{f(x)}{g(x)}=\frac{\lim_{x\to x_0}f(x)}{\lim_{x\to x_0}g(x)},$
\item $\lim_{x\to x_0}|f(x)| = |\lim_{x\to x_0}f(x)|.$
\end{enumerate}
\end{veta}

\begin{veta}[Věta o limitě funkcí shodujících se v prstencovém okolí bodu]
Nechť jsou dány funkce $f,g$ a nechť existuje prstencové okolí
$\mathscr P(x_0)$ takové, že $\forall x \in \mathscr P(x_0)\colon f(x)=g(x).$
Nechť $\lim_{x\to x_0}g(x)=A, A\in \mathbb R^*.$ Pak existuje $\lim_{x\to x_0}f(x)$
a platí $\lim_{x\to x_0}f(x)=A.$
\end{veta}

\begin{priklad}
Spočtěte $\lim_{x\to -1}\frac{x^2-1}{x+1}$.
\end{priklad}

\begin{reseni}
 Platí
 $$\lim_{x\to -1}\frac{x^2-1}{x+1}=\lim_{x\to -1}\frac{(x-1)(x+1)}{x+1}=\lim_{x\to -1}(x-1)=-2.$$
\end{reseni}

\begin{priklad}
Spočtěte $\lim_{x\to-\infty}x(\sqrt{x^2+9}-\sqrt{x^2-9})$.
\end{priklad}

\begin{reseni}
Počítejme
\begin{align*}
	\lim_{x\to-\infty}x(\sqrt{x^2+9}-\sqrt{x^2-9}) &= \lim_{x \to -\infty} x(\sqrt{x^2+9}-\sqrt{x^2-9}) \cdot \frac{\sqrt{x^2+9}+\sqrt{x^2-9}}{\sqrt{x^2+9}+\sqrt{x^2-9}}\\
	&= \lim_{x \to -\infty} x \cdot \frac{(x^2 + 9) - (x^2 - 9)}{\sqrt{x^2+9}+\sqrt{x^2-9}} \\
	&= \lim_{x \to -\infty} \frac{x}{\sqrt{x^2+9}+\sqrt{x^2-9}} \cdot \frac{\frac{1}{x}}{\frac{1}{x}} \\
	&= \lim_{x \to -\infty} \frac{1}{\sqrt{1+\frac{9}{x^2}}+\sqrt{1-\frac{9}{x^2}}} = \frac{1}{2}.
\end{align*}
\end{reseni}

\begin{veta}[Věta o sevření]
Nechť $f,g,h$ jsou funkce a nechť existuje prstencové okolí $\mathscr P(x_0)$
bodu $x_0\in \mathbb R^*$ takové, že $\forall x \in \mathscr P(x_0)\colon g(x)\leq f(x)\leq h(x).$
Nechť $\lim_{x\to x_0}g(x)=\lim_{x\to x_0}h(x)=A, A \in \mathbb R^*.$ Pak existuje
$\lim_{x\to x_0}f(x)$ a  platí $ \lim_{x\to x_0}f(x) = A.$
\end{veta}

\begin{veta}[Věta o součinu nulové a ohraničené funkce]
Nechť jsou dány dvě funkce $f,g$ a~$\lim_{x\to x_0}f(x)=0.$ Nechť existuje prstencové
okolí $\mathscr P(x_0),$ bodu $x_0\in \mathbb R^*$ takové, že
funkce $g$~je na tomto okolí ohraničená. Pak $\lim_{x\to x_0}f(x)g(x)=0.$
\end{veta}

\begin{priklad}
Spočtěte $\lim_{x\to \infty}\left ( 4x^3-x^2+x+2 \right ) .$
\end{priklad}

\begin{reseni}
Počítejme
$$
    \lim_{x\to \infty}\left ( 4x^3-x^2+x+2 \right ) = \lim_{x \to \infty}  x^3 \left( 4 - \frac{1}{x} + \frac{1}{x^2} + \frac{2}{x^3} \right ) = 4
$$
podle věty o aritmetice limit.
\end{reseni}

\begin{veta}[Věta o limitě složené funkce]\label{slozf}
Nechť $f,g$ jsou funkce a $x_0\in \mathbb R^*, A \in \mathbb R$ a nechť platí
\begin{enumerate}[$i.$]
\item $\lim_{x\to x_0}g(x)=A,$
\item funkce $f$ je spojitá v bodě $x_0.$
\end{enumerate}
Pak platí
$$\lim_{x\to x_0}f \left ( g(x) \right ) =f \left ( \lim_{x\to x_0}g(x) \right ) =f(A).$$
\end{veta}


\begin{pozn}
    Důsledkem věty \ref{slozf} je fakt, že
    $$\lim_{x\to x_0}g(x)=A \implies \lim_{x\to x_0}e^{g(x)}=e^A.$$
    To využíváme při výpočtu limit exponenciálních výrazů. Platí totiž
    $$f(x)^{g(x)}=e^{g(x)\ln f(x)}.$$
\end{pozn}

\begin{priklad}
Spočtěte $\lim_{x\to 0} \cos \left ( x^2\cdot \sin \frac{1}{x} \right ) $.
\end{priklad}

\begin{reseni}
Je
$$\lim_{x\to 0} \cos \left ( x^2\cdot \sin \frac{1}{x} \right ) =\cos 0 =1,$$
neboť $\sin \frac{1}{x}$ je omezená a $x^2$ je spojitá v $\mathbb R$.
\end{reseni}

\begin{priklad}
Spočtěte $\lim_{x\to 0^+}x^{\ln x}.$
\end{priklad}

\begin{reseni}
Platí
$$\lim_{x\to 0^+}x^{\ln x}=\lim_{x\to 0^+}e^{\ln x\ln x}.$$
Protože je $e^x$ spojitá, je
$$\lim_{x\to 0^+}e^{\ln x\ln x}=e^{\lim_{x\to 0^+}\ln x\ln x}.$$
Limita
$$
    \lim_{x\to 0^+}\ln x\ln x = \infty,
$$
a tedy i $\lim_{x\to 0^+}x^{\ln x} = \infty$ podle věty o limitě složené funkce.
\end{reseni}

\begin{veta}[Věta o limitě typu $1 / 0$]
Nechť $f$ je funkce a nechť existuje pravé prstencové okolí $\mathscr P^+(x_0)$ bodu
$x_0\in \mathbb R^*$ takové, že pro každé $x\in \mathscr P^+(x_0)$ platí
$f(x)>0$ (resp. $f(x)<0$). Nechť $\lim_{x\to x_0^+}f(x)=0.$ Pak platí
$$\lim_{x\to x_0^+} \frac{1}{f(x)}=\infty \quad (\textrm{resp. } -\infty).$$
Analogicky pro levé okolí bodu.
\end{veta}

\begin{priklad}
Spočtěte $\lim_{x\to 2^+}\frac{x}{x-2}.$
\end{priklad}

\begin{reseni}
Dosaďme a počítejme
 $$\lim_{x\to 2^+}\frac{x}{x-2}=\frac{2}{0^+}=+\infty.$$
\end{reseni}

\begin{veta}[l'Hospitalovo pravidlo]
Nechť $x_0\in \mathbb R^*$. Nechť je splněna jedna z podmínek
\begin{enumerate}[$i.$]
\item $\lim_{x\to x_0}f(x)=\lim_{x\to x_0}g(x)=0,$
\item $\lim_{x\to x_0}|g(x)| = +\infty.$
\end{enumerate}
Existuje-li $\lim_{x\to x_0}\frac{f^\prime (x)}{g^\prime (x)},$ pak existuje také
$\lim_{x\to x_0}\frac{f(x)}{g(x)}$ a platí
$$\lim_{x\to x_0}\frac{f(x)}{g(x)}=\lim_{x\to x_0}\frac{f^\prime (x)}{g^\prime(x)}.$$
\end{veta}

\begin{priklad}
  Spočtěte $\lim_{x\to 0}\frac{\sin x}{x}.$
\end{priklad}

\begin{reseni}
  Řešíme l'Hospitalovým pravidlem.
  Platí
  $$\lim_{x\to 0}\frac{\sin x}{x}=\lim_{x\to 0}\frac{\cos x}{1}=1$$
\end{reseni}


\begin{priklad}
Spočtěte $\lim_{x\to 1} \left ( \frac{1}{\ln x}-\frac{1}{x-1} \right ) $.
\end{priklad}

\begin{reseni}
Převedeme na společný jmenovatel a použijeme l'Hospitalovo pravidlo. Je
\begin{align*}
\lim_{x\to 1} \left ( \frac{1}{\ln x}-\frac{1}{x-1} \right ) &= \lim_{x \to 1} \frac{x-1-\ln x}{(x-1)\ln x} \stackrel{\text{LP}}{=} \lim_{x \to 1} \frac{1 - \frac{1}{x}}{\ln x + (x-1) \frac{1}{x}}\\
& \stackrel{\text{LP}}{=}\lim_{x \to 1} \frac{\frac{1}{x^2}}{\frac{1}{x} + \frac{1}{x} - (x-1) \frac{1}{x^2}} = \frac{1}{2}.
\end{align*}
\end{reseni}

\begin{priklad}
Spočtěte $\lim_{x\to \infty}\left ( \sqrt{x^2+x-1}-\sqrt{x^2-x+1}   \right ) $.
\end{priklad}

\begin{reseni}
Rozšíříme $\sqrt{x^2+x-1}+\sqrt{x^2-x+1}$. Je
\begin{align*}
&= \lim_{x\to \infty}\left ( \sqrt{x^2+x-1}-\sqrt{x^2-x+1}   \right ) \cdot \frac{\sqrt{x^2+x-1}+\sqrt{x^2-x+1}}{\sqrt{x^2+x-1}+\sqrt{x^2-x+1}}\\
&= \lim_{x\to \infty} \frac{(x^2+x-1)-(x^2-x+1) }{\sqrt{x^2+x-1}+\sqrt{x^2-x+1}} = \lim_{x\to \infty} \frac{-2}{\sqrt{x^2+x-1}+\sqrt{x^2-x+1}} = 0.
\end{align*}
\end{reseni}

\begin{priklad}
Spočtěte $\lim_{x\to 0^+}x^{\sin x}.$
\end{priklad}

\begin{reseni}
Platí
$$\lim_{x\to 0^+}x^{\sin x}=\lim_{x\to 0^+}e^{\sin x\ln x}=e^{\lim_{x\to 0^+}\sin x\ln x}$$
podle věty o limitě složené funkce. Je
$$
\lim_{x\to 0^+}\sin x\ln x = \lim_{x\to 0^+} \frac{\sin x }{x} \cdot x \cdot \ln x = \lim_{x\to 0^+}x \ln x = \lim_{x\to 0^+}\frac{\ln x }{\frac{1}{x}} \stackrel{\text{LP}}{=} \lim_{x\to 0^+} \frac{\frac{1}{x}}{-\frac{1}{x^2}} = \lim_{x\to 0^+}-x = 0,
$$
a tedy celkově je limita rovna $e^0 = 1$.
\end{reseni}

\begin{priklad}
  Spočtěte $\lim_{x\to 0^+}x\ln x.$
\end{priklad}

\begin{reseni}
  Převedeme do jmenovatele:
  $$\lim_{x\to 0^+}x\ln x=\lim_{x\to 0^+}\frac{\ln x}{\frac{1}{x}} = \dots = 0.$$
\end{reseni}
